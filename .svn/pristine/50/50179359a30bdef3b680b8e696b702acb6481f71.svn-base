#pragma once
#include <stdio.h>

class CMP4Writer
{
    public:
        CMP4Writer();
        ~CMP4Writer();
        void Open(const char* filepath,int width,int height,int fps);
        FILE* m_MP4Handle;
}